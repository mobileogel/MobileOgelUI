cd yolov5
pip uninstall coremlmodels
pip install coremlmodels==6.2.0
python3 export.py --weights ../best.pt --include coreml --dynamic
update .mlmodel to .mlpackage in xcode

new fix:
cd yolov5
git clone https://github.com/hietalajulius/yolov5
pip install -r requirements.txt -r requirements-export.txt
^ but first go in both files and change the coremltools version to 6.2.0
python export-nms.py --include coreml --weights ../best.pt
update .mlmodel to .mlpackage in xcode